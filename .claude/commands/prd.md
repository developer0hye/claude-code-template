Use the `prd-authoring` subagent to create, revise, or audit the repository's `PRD.md`.

Requirements:

- If `PRD.md` does not exist, create it instead of blocking.
- Follow `.claude/references/prd-standard.md` for the required structure and quality bar.
- Keep the result self-contained, exact, and verifiable.
